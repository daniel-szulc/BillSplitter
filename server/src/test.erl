%%-module(test).
%%-export([main/0]).
%%
%%main() ->
%%
%% Bills = [[20, 15, 10], [10, 20, 10], [10, 10, 20]],
%% Totals = splitter_test:split_bills(Bills),
%% io:fwrite( "Output New Arr:\n ~p \n\n", [Totals]),
%% Debts = [[0, 10, 20], [0, 0, 10], [10, 0, 0]],
%% Result1 = splitter_test:split_bills(Debts),
%% %%SplittedArray = splitter:splitBill(Debts, 3, 3),
%% %%Before = stringArray:arrayToString(3, 3, Debts, "", 0),
%% %%io:fwrite( "Input Array:\n ~p \n\n\n\n", [Before]),
%% %%stringArray:arrayToString(3, 3, SplittedArray, "", 0).
%% %%io:fwrite( "Output Array:\n ~p \n\n", [Result]).
%%
%% io:fwrite( "Output New Arr:\n ~p \n\n", [Result1]).
